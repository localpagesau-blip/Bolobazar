FROM python:3.12-slim
WORKDIR /app
COPY backend/ /app/backend/
COPY voice-agent/requirements.txt /app/
RUN pip install fastapi uvicorn sqlalchemy twilio aiohttp
WORKDIR /app/backend
RUN python init_db.py && python seed_db.py
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8001"]
